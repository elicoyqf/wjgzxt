<%= provide(:body_id, 'index') %>
<%= provide(:result, '查询负分原始记录') %>

<div class="container-fluid content-wrapper">
  <div class="row-fluid">
    <div class="span12">
      <div class="hero-unit">
        <button type="button" class="close hidden-phone" data-dismiss="alert">&times;</button>
        <h1>查询负分原始记录</h1>

        <p>
          负分原始记录可以按次统计周期查询来查看，如对结果有异议，可申请核对。
        </p>
        <br/>

        <p class="hidden-phone">
          <a class="btn btn-info btn-large pull-right">详细指南请猛击此处 &raquo;...</a>
        </p>
      </div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <div class="row-fluid hidden-desktop hidden-phone">
        <div class="span12">
          <h4 class="title">负分原始记录如下示：</h4>

          <div class="squiggly-border"></div>
          <table class="table table-striped table-bordered table-radmin">
            <thead>
            <tr>
              <th style="text-align:center;">测试时间</th>
              <th style="text-align:center;">出口名称</th>
              <th style="text-align:center;">测试网站</th>
              <th style="text-align:center;">首屏打开时间(ms)</th>
              <th style="text-align:center;">总时间(ms)</th>
              <th style="text-align:center;">吞吐率(KB/s)</th>
              <th style="text-align:center;">连接成功率(%)</th>
              <th style="text-align:center;">首页加载成功率(%)</th>
            </tr>
            </thead>
            <tbody>
            <% @out.each do |line| %>
              <tr>
                <% if line[0].blank? %>
                  <td style="text-align:center;"><%= line[0] %></td>
                <% else %>
                  <td style="text-align:center;"><%= line[0].strftime('%Y-%m-%d %H:%M:%S') %></td>
                <% end %>
                <td style="text-align:center;"><%= line[1] %></td>
                <td style="text-align:center;"><%= line[2] %></td>
                <td style="text-align:center;"><%= line[3] %></td>
                <td style="text-align:center;"><%= line[4] %></td>
                <td style="text-align:center;"><%= line[5] %></td>
                <td style="text-align:center;"><%= line[6] %></td>
                <td style="text-align:center;"><%= line[7] %></td>
              </tr>
            <% end %>
            <% @bbdata.each do |bb| %>
              <tr>
                <td style="text-align:center;"><%= bb.test_time.strftime('%Y-%m-%d %H:%M:%S') %></td>
                <td style="text-align:center;"><%= bb.source_node_name %></td>
                <td style="text-align:center;"><%= bb.dest_url %></td>
                <td style="text-align:center;"><%= bb.time_to_index %></td>
                <td style="text-align:center;"><%= bb.total_time %></td>
                <td style="text-align:center;"><%= bb.throughput_time %></td>
                <td style="text-align:center;"><%= bb.connection_sr %></td>
                <td style="text-align:center;"><%= bb.index_page_loading_sr %></td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <p></p>

  <div class="row-fluid">
    <div class="span12">
      <h4 class="title">名言警句</h4>

      <div class="squiggly-border"></div>
    </div>
  </div>

  <div class="row-fluid">
    <div class="span12">
      <blockquote>
        <p>
          Etiam varius erat in nisl egestas id auctor lacus consectetur.
          Ut dui risus, auctor a malesuada volutpat, tempus sit amet arcu.
        </p>
        <small class="pull-right">Jane Doe via Mobile</small>
      </blockquote>
    </div>
  </div>
</div>
